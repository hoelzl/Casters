{
  "extends": "../../tsconfig.json", // Adjust the path as necessary to point to your root tsconfig.json
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "casters_core/*": ["../casters_core/src/*"]
    }
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.json"
  ],
  "exclude": [
    "dist",
    "node_modules",
    "tsconfig.json"
  ]
}
